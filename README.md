# offline-dataflow-policy-auditor
Build a Python CLI that parses a small pipeline manifest, performs taint and policy analysis over its directed acyclic graph, and emits deterministic, actionable violation reports.
